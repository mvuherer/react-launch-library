import styled from 'styled-components';
import Select from 'react-select';

const Agencies = styled(Select)`
  width: 335px;
  margin-left: 52.5px;
  margin-bottom: 25px;
`;

export default Agencies;

import styled from 'styled-components';

const Header = styled.div`
  font-size: 32px;
  padding-left: 52.5px;
  margin-bottom: 25px;
  display: flex;
  align-items: center;

  > div {
    margin-left: auto;
  }
`;

export default Header;
